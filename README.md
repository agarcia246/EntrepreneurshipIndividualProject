
  # Build from attached file

  This is a code bundle for Build from attached file. The original project is available at https://www.figma.com/design/xKev6ddIIXsVCgwuThrSAV/Build-from-attached-file.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  